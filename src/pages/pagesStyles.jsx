import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  homePageWrapper: {
    textAlign: "center",
    margin: "40% 0 0 0"
  },
  noContacts: {
    textAlign: "center",
  },
  title: {
    whiteSpace: "pre-wrap",
    fontSize: 20,
    marginBottom: 24,
  }
});
